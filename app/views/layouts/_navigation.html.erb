<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
        data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only"><%= t "nav.toggle" %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <strong><%= link_to t("home.title"), "/", class: "navbar-brand" %></strong>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to t("pages.about"), "" %></li>
        <li><%= link_to t("pages.news"), "" %></li>
        <li><%= link_to t("pages.tours"), "" %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "#" do %>
            <%= t ("nav.cart") %><span class="glyphicon glyphicon-shopping-cart"></span>
            <span class="badge">0</span>
          <% end %>
        </li>
         <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"
              role="button" aria-haspopup="true" aria-expanded="false">
              <strong><%= current_user.username %></strong> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("users.edit"), edit_user_registration_path %></li>
              <li>
                <%= link_to t("users.logout"), destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <p class="navbar-btn">
              <%= link_to t("nav.signin"), signin_path, remote: true,
                class: "btn btn-default btn-sm" %>
            </p>
          </li>
          <li>
            <p class="navbar-btn">
              <%= link_to t("nav.signup"), register_path, class: "btn btn-primary btn-sm" %>
            </p>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
