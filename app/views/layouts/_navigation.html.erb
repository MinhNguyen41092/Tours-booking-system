<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
        data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only"><%= t "nav.toggle" %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand">
        <a href="#" data-toggle="collapse" id="button-collapse" aria-expanded="false"><i
          class="fa fa-bars"></i></a><strong><%= link_to t("home.title"), "/" %></strong>
      </div>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to t("pages.about"), "" %></li>
        <li><%= link_to t("pages.news"), "" %></li>
        <li><%= link_to t("pages.tours"), tours_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <% if current_user.cart.present? %>
              <%= link_to current_user.cart do %>
                <%= t ("nav.cart") %><span class="glyphicon glyphicon-shopping-cart"></span>
                <span class="badge"><%= current_user.cart.line_items.length %></span>
              <% end %>
            <% end %>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"
              role="button" aria-haspopup="true" aria-expanded="false">
              <%= image_tag current_user.avatar.avatar_nav %>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Edit settings", edit_user_registration_path %></li>
              <li><%= link_to "Your profile", user_path(current_user) %></li>
              <li class="divider"></li>
              <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li>
            <p class="navbar-btn">
              <%= link_to t("nav.signin"), signin_path, remote: true,
                class: "btn btn-default btn-sm" %>
            </p>
          </li>
          <li>
            <p class="navbar-btn">
              <%= link_to t("nav.signup"), register_path, class: "btn btn-primary btn-sm" %>
            </p>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
